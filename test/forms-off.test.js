const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
test("forms import and factory presentation helpers are byte-inert while dark",()=>{delete process.env.FORMS_ENABLED;const before={url:process.env.SUPABASE_URL,key:process.env.SUPABASE_SERVICE_KEY},html=fs.readFileSync(path.join(__dirname,"..","public","book.html"),"utf8");const x=require("../lib/forms");assert.deepEqual(Object.keys(x),["buildFormsHandlers","decorateFormsBookPage","formsClaimGateFragment","BUILTIN_FORM"]);assert.equal(x.decorateFormsBookPage(html,false),html);assert.equal(x.formsClaimGateFragment(false),"");assert.doesNotMatch(x.decorateFormsBookPage(html,false),/COMPARE_GAP:FORMS/);assert.deepEqual({url:process.env.SUPABASE_URL,key:process.env.SUPABASE_SERVICE_KEY},before)});
