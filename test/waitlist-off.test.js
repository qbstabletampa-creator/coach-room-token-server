const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
test("waitlist import and factory presentation helper are byte-inert while dark",()=>{delete process.env.WAITLIST_ENABLED;const before={url:process.env.SUPABASE_URL,key:process.env.SUPABASE_SERVICE_KEY},html=fs.readFileSync(path.join(__dirname,"..","public","book.html"),"utf8");const mod=require("../lib/waitlist");assert.deepEqual(Object.keys(mod),["buildWaitlistHandlers","decorateWaitlistBookPage"]);assert.equal(mod.decorateWaitlistBookPage(html,false),html);assert.doesNotMatch(mod.decorateWaitlistBookPage(html,false),/COMPARE_GAP:WAITLIST/);assert.deepEqual({url:process.env.SUPABASE_URL,key:process.env.SUPABASE_SERVICE_KEY},before);});
