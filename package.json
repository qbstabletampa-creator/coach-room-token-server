{
  "name": "coach-room-token-server",
  "version": "1.0.0",
  "description": "Mints LiveKit access tokens for the Coach Room live coaching room. Server-side only — holds the LiveKit API secret.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/clips.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "livekit-server-sdk": "^2.15.4"
  }
}
